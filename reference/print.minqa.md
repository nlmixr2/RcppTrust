# Print method for minqa objects

Print method for minqa objects

## Usage

``` r
# S3 method for class 'minqa'
print(x, digits = max(3, getOption("digits") - 3), ...)
```

## Arguments

- x:

  an object inheriting from class `minqa`.

- digits:

  unused; kept for compatibility with `minqa`.

- ...:

  unused.

## Value

`invisible(x)`
