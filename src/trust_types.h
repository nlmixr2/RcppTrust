#ifndef TRUST_TYPES_SRC_FORWARD_H
#define TRUST_TYPES_SRC_FORWARD_H
/* Canonical definitions live in inst/include/trust_types.h so downstream
 * consumers (via LinkingTo) and this package's own src/ build share
 * exactly one copy -- see inst/include/RcppTrust.h for how a consumer
 * uses these types. */
#include "../inst/include/trust_types.h"
#endif
